{
  "name": "olvra-boost",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/generate-config.js && node scripts/bundle-firebase.js && node scripts/bump-sw-version.js",
    "cap:add:android": "cap add android",
    "cap:sync": "cap sync",
    "cap:open:android": "cap open android"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase": "10.12.0",
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "esbuild": "^0.21.0",
    "@capacitor/core": "^6.1.0",
    "@capacitor/cli": "^6.1.0",
    "@capacitor/android": "^6.1.0",
    "@capacitor/push-notifications": "^6.0.0",
    "@capacitor/splash-screen": "^6.0.0"
  }
}
